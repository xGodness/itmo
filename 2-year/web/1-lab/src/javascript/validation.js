function validate() {
    let raw_y = document.getElementById("input-y").value;
    let raw_r = document.getElementById("input-r").value;

    let y = parseFloat(raw_y);
    let r = parseFloat(raw_r);

    if (Number.isNaN(y) || Number.isNaN(r)) {

    }
}